# Aurexillion Frontend

This package contains the Next.js ticket board UI.

For installation, environment setup, running the app, database seeding, tests, and architecture notes, see the [root README](../README.md).

```bash
pnpm install
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000).
